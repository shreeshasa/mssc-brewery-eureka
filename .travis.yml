language: java
os: linux
jdk:
  - openjdk12
services:
  - docker
cache:
  directories:
    - $HOME/.m2
# skip installation step
install: true
script:
  - ./mvnw clean package -B -Pdocker
  - ./mvnw --settings .maven.xml docker:push -Pdocker